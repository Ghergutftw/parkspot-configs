eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka/
  instance:
    prefer-ip-address: true

infra:
  postgres:
    host: postgres
    port: 5432
  redis:
    host: redis
    port: 6379
  kafka:
    bootstrap-servers: kafka:9092
  s3:
    region: ${AWS_REGION:eu-central-1}
    access-key: ${AWS_ACCESS_KEY_ID:}
    secret-key: ${AWS_SECRET_ACCESS_KEY:}
    bucket: ${S3_BUCKET:park-spot}

# browser-facing URLs stay on localhost: docker-compose publishes ports to the host,
# and the browser talks to the gateway/auth-server from outside the compose network.
# internal-issuer-uri is for server-to-server calls (JWKS fetch, client-credentials token
# fetch) which happen from inside the compose network, where "localhost" would resolve to
# the calling container itself rather than auth-server.
parkspot:
  web-origin: http://localhost:4200
  internal-issuer-uri: http://auth-server:9000

management:
  otlp:
    tracing:
      endpoint: http://tempo:4318/v1/traces
