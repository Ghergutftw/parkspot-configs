server:
  port: 8080

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${parkspot.issuer-uri:http://localhost:9000}
  cloud:
    gateway:
      mvc:
        routes:
          # auth-server's own controllers are already mapped under /api/auth/**, so no prefix is stripped.
          - id: auth-server
            uri: lb://auth-server
            predicates:
              - Path=/api/auth/**
          - id: parking-service
            uri: lb://parking-service
            predicates:
              - Path=/api/parking/**
            filters:
              - StripPrefix=1
          - id: reservation-service
            uri: lb://reservation-service
            predicates:
              - Path=/api/reservations/**
            filters:
              - StripPrefix=1
          - id: messaging-service
            uri: lb://messaging-service
            predicates:
              - Path=/api/conversations/**
            filters:
              - StripPrefix=1
          - id: notification-service
            uri: lb://notification-service
            predicates:
              - Path=/api/notifications/**
            filters:
              - StripPrefix=1

parkspot:
  issuer-uri: ${AUTH_ISSUER_URI:http://localhost:9000}
  cors:
    allowed-origins: ${parkspot.web-origin:http://localhost:4200}
  oauth2:
    client-id: api-gateway
    client-secret: ${GATEWAY_CLIENT_SECRET:local-only-gateway-secret}
